import React, { useState } from 'react';
import Nav from './Nav';

function Header() {
    return (
        <div id="header">
            <h1>Portfolio Header</h1>
            <Nav />
        </div>
    );
}

export default Header;